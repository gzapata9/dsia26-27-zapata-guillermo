# 20 oct 2026 — E2E I: Ingesta → Procesamiento → API de IA + Trabajo Final

**Material:** `5_desarrollo_end_to_end/01_arquitectura_e2e.md` · `plantilla_proyecto/` · `proyectos/proyecto_iii/` · `ejercicios/E8_e2e.md`

| Bloque | Min |
| --- | --- |
| Exposición + demo | 0–30 |
| Ejercicios | 30–60 |
| Presentación Trabajo Final | 60–105 |

---

## Objetivos

1. Ensamblar un flujo completo localmente.
2. Exponer resultados vía HTTP (`/health`, `/analyze`).
3. Entender el alcance y rúbrica del **Proyecto III (40 %)**.
4. Dejar un esqueleto runnable como base del Trabajo Final.

---

## Bloque A — Exposición (30 min)

### A1. Visión E2E (5 min)

```text
[CSV/API] → Ingesta/validación → Procesamiento → Cliente IA → API → Usuario
                              ↘ logs/métricas
```

Qué evalúa el Proyecto III: valor de la solución + arquitectura + IA + tests + despliegue + exposición.

### A2. Plantilla del curso (12 min) — live

```bash
cd 5_desarrollo_end_to_end/plantilla_proyecto
python ingest.py --input ../../3_automatizacion_e_ia/Datos/ventas.csv --output data/clean.csv
python app.py
```

En otra terminal:

```bash
curl -s http://127.0.0.1:8000/health
curl -s -X POST http://127.0.0.1:8000/analyze \
  -H 'Content-Type: application/json' \
  -d '{"text":"Texto de ejemplo suficientemente largo para el resumen mock del curso DSIA"}'
pytest -q
```

Explicar responsabilidades de `ingest.py` vs `app.py` vs tests.

### A3. Contratos de API (8 min)

- Request JSON validado → 400 si falta `text`.
- Response estable: `{summary, provider}`.
- `/health` para orquestadores y PaaS.
- Dónde sustituir el mock por `ai_api_client.complete(...)`.

### A4. Arranque del Trabajo Final (5 min)

Mostrar `proyectos/proyecto_iii/README.md`: fechas (RC 17 nov, expos 1/8 dic), requisitos mínimos, rúbrica.

---

## Bloque B — Ejercicios (30 min)

| Min | Checkpoint |
| --- | --- |
| 0–8 | Clonar/copiar plantilla a carpeta de proyecto personal |
| 8–18 | Sustituir `summarize` mock por llamada a `complete("mock", ...)` importada |
| 18–25 | Añadir campo `model` opcional al JSON de respuesta |
| 25–30 | Test nuevo: texto vacío → 400 |

**Hecho:** API responde con `provider: mock` y tests en verde.

---

## Bloque C — Presentación Trabajo Final (~45 min)

Enunciado + Q&A + ideación: cada estudiante/equipo define problema, datos e integración de IA. Entregable del día: issue/README con propuesta de 10–15 líneas.
